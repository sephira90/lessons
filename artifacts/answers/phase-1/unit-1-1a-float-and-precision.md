# Unit 1.1a Answers

## Предсказания

1. `0.1 + 0.2` ->
2. `(0.1 + 0.2) === 0.3` ->
3. `(0.1 + 0.2) == 0.3` ->
4. `abs((0.1 + 0.2) - 0.3) < 1e-12` ->
5. `0.3 - 0.2` ->
6. `(0.3 - 0.2) === 0.1` ->
7. `0.1 * 10` ->
8. `(0.1 * 10) === 1.0` ->
9. `$sum = 0.0; repeat 10 times { $sum += 0.1; }` ->
10. `abs($sum - 1.0) < 1e-12` ->
11. `1.0 == 1` ->
12. `1.0 === 1` ->
13. `1.5 == '1.5'` ->
14. `1.5 === '1.5'` ->
15. `0.0 == false` ->
16. `1.0 == true` ->
17. `0.0 == null` ->
18. `1.0 == null` ->
19. `1.5 == 'foo'` ->

## Разбор ошибок

- Какие предсказания оказались неверными:
- Какая ложная модель к ним привела:
- Какая модель лучше объясняет фактический результат:

## Ключевые ответы

- Почему `(0.1 + 0.2) === 0.3` даёт `false`:
- Почему `==` здесь тоже не спасает:
- Почему сравнение через `epsilon` полезнее строгого сравнения:
- Почему `0.1 * 10 === 1.0` может быть `true`, но это не отменяет проблему `float`:
- Почему деньги нельзя бездумно хранить во `float`:
- Почему `1.0 == 1`, но `1.0 === 1`:
- Почему `0.0 == false` и `0.0 == null` опасны:

## Blind Reconstruction

- Что такое `float`:
- Что такое `epsilon`:
- Когда `===` для `float` опасен:
- Где `float` допустим, а где лучше выбрать другой тип:
- Что добавляет к риску `float` сравнение с `string`, `bool` и `null`:
