# Tools

Local project binaries live here and are not committed.

Run Composer through the Windows wrapper:

```powershell
.\scripts\composer.cmd --version
```

The wrapper exists because Composer in this environment needs a writable local temp directory.
